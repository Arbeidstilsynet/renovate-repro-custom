# renovate-repro-custom

`customManagers:dockerfileVersions` does not work, regardless of using the preset or adding the customManager directly.

Renovate debug logs do not reference the repology datasource or the old versions in the Dockerfile at all.

<https://docs.renovatebot.com/modules/datasource/repology/>
<https://docs.renovatebot.com/presets-customManagers/#custommanagersdockerfileversions>
